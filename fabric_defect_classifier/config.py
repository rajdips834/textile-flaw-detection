# config.py

batch_size = 16        # Smaller batch size for better convergence on small datasets
learning_rate = 0.0005 # Lower learning rate for Adam optimizer
epochs = 10            # More epochs for deeper training
image_size = 64        # Input image size

num_classes = 5        # Number of defect classes
